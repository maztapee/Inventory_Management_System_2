export enum DefaultImage {
    default = "No image available",
  }


export enum DefaultDescription {
    default = "No description added"
  }

export enum DefaultFeature{
    default = "No feature added"
}